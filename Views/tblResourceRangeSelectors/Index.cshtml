@model IEnumerable<WASA_EMS.Models.tblResourceRangeSelector>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ParameterID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MinRange)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaxRange)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanyID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tblResource.MobileNumber)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ParameterID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MinRange)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaxRange)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tblResource.MobileNumber)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.RangeID }) |
                @Html.ActionLink("Details", "Details", new { id=item.RangeID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.RangeID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
